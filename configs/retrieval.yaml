search:
  default_top_k: 8
  default_symbol_boost: 2.0
  preview_chars: 160

explain:
  default_top_k: 6
  max_ctx_chars: 6000
  max_chunk_chars: 1200
  temperature: 0.2
  max_tokens: 700

agent:
  default_top_k: 6
  default_max_tokens: 512
  role: optional_enhancement

notes:
  - "These values describe stable defaults for the current release."
  - "Not every field is dynamically loaded yet."
